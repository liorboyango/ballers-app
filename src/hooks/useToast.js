/**
 * useToast hook — re-exported from ToastContext for convenient imports.
 *
 * Usage:
 *   import { useToast } from '../hooks/useToast';
 *   const { success, error } = useToast();
 */
export { useToast } from '../context/ToastContext';
