/**
 * CheckoutPage.jsx
 * Full checkout experience with Stripe Elements integration.
 * This file delegates to CheckoutPage.js (the primary implementation).
 * Kept for compatibility — re-exports the Stripe-integrated CheckoutPage.
 */
export { default } from './CheckoutPage';
