/**
 * Header.jsx — stub re-export.
 * Header.js is the single source of truth per UI/UX design spec.
 * This file exists only for compatibility; all logic lives in Header.js.
 */
export { default } from './Header.js';
