# API base URL for the backend
REACT_APP_API_URL=http://localhost:5000/api

# Payments use Rapyd Hosted Checkout (redirect flow). The backend holds the
# Rapyd secret/access keys and creates Checkout Pages via the Rapyd API; the
# frontend only redirects the user to the returned redirect_url. No Rapyd key
# is needed in this React app.
